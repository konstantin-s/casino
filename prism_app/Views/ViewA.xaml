<UserControl x:Class="prism_app.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel Background="Gainsboro" Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Center">
        <Label>ViewA</Label>
        <TextBlock Text="Представьтесь, пожалуйста:" FontSize="18" />
        <TextBox MinWidth="250" MaxWidth="250" Background="Aqua"
                 Style="{StaticResource TextBoxInError}"
                 Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
            <TextBox.Text>
                <Binding Path="PlayerName"
                         UpdateSourceTrigger="PropertyChanged"
                         ValidatesOnNotifyDataErrors="True"
                         Mode="TwoWay" />
            </TextBox.Text>
        </TextBox>

        <Button Content="Начать игру" Background="{x:Null}"
                Command="{Binding DoIdentificate}"
                IsEnabled="{Binding IsIdentificateAllowed}"
                Margin="0 20 0 0" />
    </StackPanel>
</UserControl>